# Models

Place optimized GLBs here when Asset Gate completes:

- `hand-hero.glb`
- `hand-explore.glb`
- `hand-mobile.glb`

Node naming contract: `../../lib/glb-contract.ts`

Do not commit raw STEP files here.
